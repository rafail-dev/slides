commit-msg:
  commands:
    commit-lint:
      run: bun commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    format-check:
      glob: "*.{ts,vue,js,mjs,mts,json,md,yml}"
      run: bun prettier --check {staged_files}
